json.extract! insumo, :id, :fecha, :num_fact, :proveedor, :documento, :numero, :empleado, :cant, :detalle, :val_uni, :total, :subtotal, :iva, :total, :created_at, :updated_at
json.url insumo_url(insumo, format: :json)
