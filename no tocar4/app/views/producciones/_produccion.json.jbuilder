json.extract! produccion, :id, :fecha, :num_fact, :cliente, :fecha_entre, :empleado, :cant, :prenda, :pieza, :talla, :val_uni, :total, :subtotal, :iva, :total, :created_at, :updated_at
json.url produccion_url(produccion, format: :json)
